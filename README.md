# Ledgerstone

A database engine built from scratch: storage, indexing, a query layer, and crash recovery, with readable internals.

## Status

Early stage. Project scaffolding only, implementation not started yet.
